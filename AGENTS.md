Project context managed under `docs/`. Navigate by `docs/index.md`.

Use OpenSpec features only when explicitly asked.

Do not use browser or Playwright testing unless the user explicitly asks for it.

Write commit messages in Conventional Commits style, such as `feat: ...` or `fix: ...`.
